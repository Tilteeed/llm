---
# ============================================================
# defaults/main.yml
# Роль: langchain_runtime
#
# Значения по умолчанию для создания отдельного venv и
# установки LangChain-стека.
# ============================================================

# Версия Python, которой создается LangChain venv.
# Эта версия уже должна быть установлена на хосте, например
# ролью python_runtime.
langchain_runtime_python_version: "3.11"

# Путь к отдельному venv для LangChain.
langchain_runtime_venv_path: "/opt/venvs/langchain311"

# Владелец и группа venv.
langchain_runtime_venv_owner: root
langchain_runtime_venv_group: root

# Обновлять ли pip stack внутри LangChain venv.
langchain_runtime_upgrade_pip_stack: true

# Основной стек пакетов.
langchain_runtime_packages:
  - langchain
  - langchain-community
  - langgraph

# Дополнительные пакеты, если понадобятся позже.
langchain_runtime_extra_packages: []

# Нужно ли сразу ставить пакеты.
langchain_runtime_install_packages: true